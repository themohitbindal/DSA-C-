#include <iostream>

using namespace std;

int main()
{
    int n;
    int totalDigits = 0; // Initialize totalDigits to 0
    cout << "Enter an integer: "; // Prompt for input
    cin >> n;                     // Read an integer input
    // while (n > 0)
    // {
    //     int rem = n % 10; // Get the last digit
    //     n = n / 10;       // Remove the last digit
    //     totalDigits++;    // Increment the count of digits
    // }
    for (int i = 0; i < 3; i++)
    {
       
    }
    

    return 0; // Placeholder for day7.cpp
}